# math-in-py

Implements a simple cli math interperter in python.<br>
Make it executable and add it to your $PATH to make using it easier.<br>
Doesn't use any exec()/eval()<br>
Make it executable and add it to your $PATH to make using it easier.<br>
You might have to install pyreadline3 to run it.